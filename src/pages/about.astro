---
import Layout from '../layouts/Layout.astro';
import { getCollection, render } from 'astro:content';

const aboutEntries = await getCollection('about');
const about = aboutEntries.length > 0 ? aboutEntries[0] : null;
const { Content } = about ? await render(about) : { Content: null };
---

<Layout
  title="About • Hazel"
  description="Learn more about Hazel, product designer crafting thoughtful digital experiences."
>
  <div class="about-wrapper">
    <header class="about-hero">
      <div class="avatar-container">
        {about?.data.avatar && (
          <img src={about.data.avatar} alt={about.data.name || 'Hazel'} class="avatar-img" />
        )}
      </div>

      <div class="hero-titles">
        <span class="editorial-cue">about</span>
        <h1 class="about-name">{about?.data.name || 'Hazel'}</h1>
        <p class="about-meta">Product Designer <span aria-hidden="true">&bull;</span> Based in Singapore &bull; Working globally</p>
      </div>
    </header>

    <section class="about-prose">
      {Content ? <Content /> : <p>Welcome to my portfolio!</p>}
    </section>
  </div>
</Layout>

<style>
  .about-wrapper {
    max-width: 760px;
    margin: 0 auto;
  }

  .about-hero {
    display: grid;
    grid-template-columns: minmax(120px, 150px) 1fr;
    align-items: center;
    gap: clamp(2rem, 8vw, 5rem);
    padding: 1rem 0 2.75rem;
  }

  .avatar-container {
    flex-shrink: 0;
    grid-column: 1;
    grid-row: 1;
  }

  .avatar-img {
    width: 100%;
    aspect-ratio: 4 / 5;
    border-radius: var(--radius-sm);
    object-fit: cover;
  }

  .hero-titles {
    display: flex;
    flex-direction: column;
    align-items: flex-start;
    gap: 0.6rem;
    grid-column: 2;
    grid-row: 1;
  }

  .about-name {
    font-size: clamp(2.25rem, 4vw, 3rem);
    font-weight: 700;
    letter-spacing: -0.03em;
    line-height: 1.3;
    color: var(--color-text);
  }

  .about-meta {
    font-size: 0.95rem;
    color: var(--color-text-secondary);
    line-height: 1.5;
  }

  /* Bio Prose */
  .about-prose {
    font-size: 1.15rem;
    line-height: 1.75;
    color: var(--color-prose);
    margin: 0;
    padding: 0 0 1rem;
  }

  .about-prose :global(p) {
    margin-bottom: 1.75rem;
  }

  @media (max-width: 640px) {
    .about-hero {
      grid-template-columns: 1fr;
      gap: 2rem;
      padding-bottom: 2.5rem;
    }

    .avatar-container {
      grid-column: 1;
      grid-row: 1;
      max-width: 150px;
    }

    .hero-titles {
      grid-column: 1;
      grid-row: 2;
    }

    .about-name {
      font-size: 2.25rem;
    }

    .about-prose {
      font-size: 1.1rem;
    }
  }
</style>
